language: node_js
node_js:
    - "12"

addons:
    apt:
        packages:
            - exiftran
            - libimage-exiftool-perl
            - coreutils

script:
    - npm run test
    - cd test/data && ../../file-organizer.sh dump && echo "*** ok ***"; cd ../../
    - npm run style
