language: node_js
node_js:
    - "12"

addons:
    apt:
        packages:
            - exiftran
            - libimage-exiftool-perl
            - coreutils

before_script:
    - cd test/data && ../../file-organizer.sh dump && echo "*** ok ***"

script:
    - npm run test
    - npm run style
